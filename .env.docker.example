# Copy to .env.docker for local Docker development.

# Public ports on your machine
CLIENT_PORT=3000
SERVER_PORT=8000
# Host port only. Container still uses DATABASE_PORT=5432.
# 5433 avoids clashes with a local Postgres installed on your machine.
POSTGRES_PORT=5433
REDIS_PORT=6379

# Local Docker database
POSTGRES_USER=codewithkasa
POSTGRES_PASSWORD=change-me-local
POSTGRES_DB=kasa_enterprise
POSTGRES_VOLUME_NAME=kasa-enterprise-dev_postgres_data
REDIS_VOLUME_NAME=kasa-enterprise-dev_redis_data

# Server app
NODE_ENV=development
APP_URL=http://localhost:8000
APP_PORT=8000
FRONT_END_URL=http://localhost:3000
API_VERSION=0.1.1

# NestJS database settings
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_USER=codewithkasa
DATABASE_PASSWORD=change-me-local
DATABASE_NAME=kasa_enterprise
DATABASE_SYNC=true
DATABASE_AUTO_LOAD=true

# Auth
JWT_SECRET=change-me-local-jwt-secret
JWT_TOKEN_AUDIENCE=http://localhost:3000
JWT_TOKEN_ISSUER=http://localhost:8000
JWT_ACCESS_TOKEN_TTL=3600
JWT_REFRESH_TOKEN_TTL=86400

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# Encryption
APP_ENCRYPTION_KEY=change-me-local-encryption-key

# License portal
# Use localhost here. When the API runs inside Docker, Kasa automatically routes
# this to the host machine internally.
LICENSE_PORTAL_URL=http://localhost:5000
LICENSE_PRODUCT_SLUG=kasa-enterprise

# Client public runtime
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_BASE_URL=http://server:8000
